export interface iDataEditUser {
  name: string;
  email: string;
  password: string;
  bio: string;
  image: string;
}

export type iKeys = "name" | "email" | "password" | "password" | "bio" | "image";
